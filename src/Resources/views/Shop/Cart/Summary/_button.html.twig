{% if counted == 8 %}
{% else %}
        {% if customer == null %}
        {% else %}
                    <button data-bb-mc-newcart class="ui right floated black button" data-bb-max="8" data-bb-current="{{ counted }}"
                            data-url-new="{{ path('bitbag_sylius_multi_cart_plugin_new_cart') }}"
                            data-url-update="{{ path('bitbag_sylius_multi_cart_plugin_ajax_cart_content') }}">
                        <i class="icon add"></i>
                        {{ 'bitbag_sylius_multicart_plugin.ui.add_new_cart'|trans }}
                    </button>

        {% endif %}
{% endif %}
